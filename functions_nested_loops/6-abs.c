#include "main.h"
#include <stdio.h>
/**
 * _abs - function computes the absolute value of an int
 * @a: int to compute
 * Return: 0
 */
int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
